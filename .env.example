# Local development
DEBUG=True
SECRET_KEY=change-me-for-local-dev
ALLOWED_HOSTS=127.0.0.1,localhost

# Optional local Postgres (if set, it will be used instead of sqlite)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
